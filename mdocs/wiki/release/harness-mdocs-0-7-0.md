---
id: "harness-mdocs-0-7-0"
title: "harness-mdocs 0.7.0"
category: "release"
created: "2026-07-02"
updated: "2026-07-02"
related_initiatives: ["installer-feedback-validate-docs-aliases-audit"]
tags: ["release","0.7.0","installer-feedback","validation","audit","aliases","docs"]
---

# harness-mdocs 0.7.0

Minor release planned for installer-feedback improvements.

## Added
- Validation summary fields: top-level and per-section `errorCount`, `warningCount`, and `clean`.
- `mdocs validate --human` / `--summary` summary-first output for pre-restart smoke tests and CI readability.
- Initiative `aliases` frontmatter support for renamed/merged initiative references, with graph alias info notes and did-you-mean hints.
- Audit configuration via `.mdocs.json` and environment: `level`, `maxBytes`, and `maxBackups`.

## Changed
- README installer guidance now documents restart requirements, Claude Code scope tradeoffs, config-before-install ordering, headless plugin-cache validate smoke test, validation semantics, migration cleanup, and HTTPS marketplace URL guidance.

## Compatibility
- JSON `mdocs validate` remains default and keeps existing structure while adding summary fields.
- Audit defaults preserve prior full logging and 10MB/3-backup rotation unless configured.

## Verification
- `npm run release:check` passed.
- 467 Jest tests passed.
- Coverage passed (global branch 80.25%).
- `mdocs_validate` / `node dist/cli/index.js validate` returned `valid=true`, `errors=0`, `warnings=3`; remaining warnings are known initiative INDEX false positives for `overview.md`, `log.md`, and `index.md`.
- `npm pack --dry-run` produced `harness-mdocs-0.7.0.tgz` with 298 files, 774.6 kB package size, 4.8 MB unpacked size.

## Publish Handoff
- After PR merge to `main`, tag `v0.7.0` and push the tag to trigger publish workflow.

## Referenced By

*Auto-generated by mdocs*

- installer-feedback-validate-docs-aliases-audit
