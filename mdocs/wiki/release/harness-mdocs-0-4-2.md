---
id: "harness-mdocs-0-4-2"
title: "harness-mdocs 0.4.2"
category: "release"
created: "2026-06-23"
updated: "2026-06-23"
related_initiatives: ["prepare-release-0-4-2"]
tags: ["release","0.4.2","claude-code"]
lifecycle: "stable"
knowledge_type: "reference"
confidence: "high"
source_initiatives: ["prepare-release-0-4-2","workflow-state-machine-has-no-driver","fix-claude-code-install-template-and-cli-help"]
---

# harness-mdocs 0.4.2

Released 2026-06-22. Tag `v0.4.2`, npm `harness-mdocs@0.4.2`, GitHub Release auto-generated.

## Shipped
This release lands the claude-code surface fixes found by dogfooding the surface on this repo:

1. **Drivable workflow state machine** (`workflow-state-machine-has-no-driver`). `WorkflowEngine.advance()`/`resumeAt()` had zero callers, so `currentStep` stayed IDLE and PreToolUse enforcement never bound. Added `workflow.advance` registry command, `mdocs step <step>` CLI, `mdocs_advance` MCP tool, `operations.advance()`. Bash gate switched whitelist -> destructive blacklist so build/test runs at VERIFY.
2. **Manual-install template** (`fix-claude-code-install-template-and-cli-help`). Split MCP into new `mcp.json` template (Claude Code reads project MCP from `.mcp.json`, not `.claude/settings.json`); CLI usage now lists `mcp` and `step`.

## Verification
`npm run release:check` clean (build + lint + 293 tests + coverage + mdocs validate + pack:check). Tarball `harness-mdocs-0.4.2.tgz`, 111.2 kB.

## Process notes
- Code landed via PR #1 (feature -> staging) and PR #2 (staging -> main); both merged.
- Release commit `chore(release): prepare 0.4.2` (`35cc733`) direct on main; tag `v0.4.2` pushed, triggering `publish.yml` (npm publish + GitHub Release).
- Version refs bumped: package.json, package-lock.json, .claude-plugin/marketplace.json, claude-code plugin.json. Codex plugin.json left at 0.1.1 (separately versioned).
- First release driven through the full mdocs workflow (IDLE -> COMPLETE) using the new `mdocs step` command.

## Referenced By

*Auto-generated by mdocs*

- prepare-release-0-4-2
