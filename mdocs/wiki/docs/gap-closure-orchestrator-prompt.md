---
id: "gap-closure-orchestrator-prompt"
title: "Gap Closure Orchestrator Prompt"
category: "docs"
created: "2026-06-23"
updated: "2026-06-23"
related_initiatives: ["close-harness-mdocs-gaps"]
tags: ["agent-prompt","orchestrator","0.4.3","0.5.0","gap-closure"]
---

# Claude Code Agent Prompt — Close harness-mdocs 0.4.3 / 0.5.0 Gaps

**Role:** You are a senior contributor on `bbaaxx/harness-mdocs`. Your job is to execute the Gap Closure Spec (source: functional comparison vs. a mature local mdocs process) and land the assigned changes in release order.

**Source material already in the repo:**
- Nine active initiatives in `mdocs/initiatives/` created from the spec:
  - `claude-code-sessionstart-orientation--2026-06-23.md` (G1, 0.4.3)
  - `wiki-ingest-and-compiled-knowledge-model--2026-06-23.md` (G2, 0.5.0)
  - `configurable-workflow-enforcement--2026-06-23.md` (G3, 0.4.3)
  - `lifecycle-parity-graduate-and-status--2026-06-23.md` (G4, 0.5.0)
  - `directory-v2-maintained-index-optin--2026-06-23.md` (G5, 0.4.3)
  - `unify-project-root-resolution--2026-06-23.md` (G6, 0.4.3)
  - `dogfood-enforcement-end-to-end--2026-06-23.md` (G7, 0.4.3)
  - `consumer-layering-docs--2026-06-23.md` (G8, 0.4.3)
  - `remove-self-referential-devdependency--2026-06-23.md` (G9, 0.4.3)
- Each initiative contains Objective, Plan, Context, Acceptance Criteria, Progress Log.
- Linter lives at `src/core/validation/linter.ts`; initiatives must score ≥4.

**Release split:**
- **0.4.3 (patch):** G1, G3, G5, G6, G7, G8, G9 — additive, defaults preserve 0.4.2 behavior.
- **0.5.0 (minor):** G2, G4 — new commands and changed lifecycle semantics.

**Execution order (do not skip):**
1. **G7 first** — dogfood the workflow driver in a real Claude Code session. Record friction; feed findings into G3.
2. **G6 + G9** — cheap, independent.
3. **G1 + G5 + G8** — additive 0.4.3 work.
4. **G3** — config layer; informed by G7 friction log.
5. **G2 + G4** — 0.5.0 semantic work. G2 is largest; split into sub-initiatives if scope grows.

**Workflow discipline:**
- Use `mdocs` commands to advance initiatives: `mdocs command initiative.update` with `progressNote`.
- Update each initiative's Progress Log as you complete plan items.
- Mark initiatives `done` only after their Acceptance Criteria are met and verification passes.
- For done initiatives that produced durable reusable knowledge, create/link a stable wiki entry so the linter's `done-initiative-needs-stable-learning` rule passes.

**Verification gates for every code change:**
- `npm run build` passes.
- `npm run quality` passes (or `npm run lint && npm test`).
- `npm run release:check` passes before any release commit.
- `mdocs_validate` passes.
- New tests cover the change; existing tests still pass.
- For 0.4.3 changes: confirm defaults match 0.4.2 behavior (no breaking change for existing installs).

**Specific reminders:**
- **G1 SessionStart:** emit compact `additionalContext` JSON on stdout; fail-open (exit 0) on error. Verify exact stdout contract against Claude Code docs.
- **G2 wiki.ingest:** do NOT auto-generate synthesis prose. Record operations; let the agent author prose. Coordinate with G4 (status) and G5 (index ownership).
- **G3 enforcement:** default must remain `gate`. Document config precedence (env > file > detected contract).
- **G4 lifecycle:** keep flat-v1 `done` alias intact; surface `complete` for directory-v2 without breaking normalization.
- **G5 index:** default remains external-owned/no-op. Opt-in `harness` owner only.
- **G6 root resolution:** shared helper in `src/core/`; apply to MCP server + all hooks.
- **G7 dogfood:** output is the friction log, not code. Allocate real session time.
- **G8 docs:** explain additive composition; do not instruct consumers to edit package internals.
- **G9 hygiene:** remove `harness-mdocs` from `devDependencies`; verify no published-name imports remain.

**Communication:**
- Report status after each gap: what changed, test results, and which initiative you advanced.
- If a gap must be split or a plan item changes, update the initiative file via `mdocs command initiative.update` with a clear progress note before proceeding.

Start by reading `mdocs/initiatives/dogfood-enforcement-end-to-end--2026-06-23.md` and driving G7.


## Referenced By

*Auto-generated by mdocs*

- close-harness-mdocs-gaps
