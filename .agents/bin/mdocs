#!/usr/bin/env sh
set -eu

ROOT="$(CDPATH= cd -- "$(dirname "$0")/../.." && pwd)"

if [ ! -f "$ROOT/dist/cli/index.js" ]; then
  echo "mdocs CLI is not built. Run: npm run build" >&2
  exit 1
fi

exec node "$ROOT/dist/cli/index.js" "$@"
